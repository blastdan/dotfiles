# Global mise configuration
# Manages runtimes, environment variables, and secrets

[tools]
python = "3.13"
node = "22"

[env]
# No _.source here — secrets.sh is sourced once directly in .zshrc
# to avoid mise re-running it on every prompt (would cause ~1s lag per command)
